#include <iostream>
#include<list>  
using namespace std;


//Kesken
void funktio(string s) {
    int pituus = s.length();
    list<string> lista;
    for (int i = 0; i < pituus; i++) {
        //a
        char uusi[pituus];
        uusi[0] = s[i];


    }
}

int main() {
    string s = "ab";
    funktio(s);
}